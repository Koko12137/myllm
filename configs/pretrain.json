{
    "dataset": "datasets/BAAI/IndustryCorpus2/merge", 
    "output": "output", 
    "replace": true, 
    "training_args": {
        "per_device_train_batch_size": 24, 
        "num_train_epochs": 5, 
        "gradient_accumulation_steps": 1, 
        "dataloader_num_workers": 10, 
        "dataloader_persistent_workers": true, 
        "use_cpu": false, 
        "deepspeed": "configs/deepspeed.json"
    }
}